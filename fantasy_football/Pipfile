[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

# For linux cpu: pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# For windows cpu: pip3 install torch torchvision torchaudio
# pip3 install notebook
[packages]
notebook = "*"
torch = "2.0.1+cpu"
torchaudio = "2.0.2+cpu"
torchvision = "0.15.2+cpu"

[dev-packages]

[requires]
python_version = "3.10"
